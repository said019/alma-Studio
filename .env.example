# ============================================================
# ALMA MOVEMENT — Variables de Entorno
# Copia este archivo a .env y rellena los valores reales.
# NUNCA subas .env a git.
# ============================================================

# Backend API (URL base del backend en Railway una vez desplegado)
VITE_API_URL=https://<tu-backend>.up.railway.app/api

# URL pública de la app (usada en links de emails: reset password, magic links, etc.)
APP_URL=https://alma-movement.com.mx

# URL base de assets públicos/email branding (si no se define, usa APP_URL)
SITE_URL=https://alma-movement.com.mx

# Orígenes permitidos por CORS (separados por coma)
CORS_ALLOWED_ORIGINS=https://alma-movement.com.mx,http://localhost:5173,http://127.0.0.1:5173

# Rate limiting API (opcional)
API_RATE_LIMIT_WINDOW_MS=60000
API_RATE_LIMIT_MAX=180
AUTH_RATE_LIMIT_WINDOW_MS=60000
AUTH_RATE_LIMIT_MAX=20

# Base de datos PostgreSQL (solo para scripts de migración locales, NO exponer en frontend)
DATABASE_URL=postgresql://postgres:<password>@<host>:<port>/railway

# Seguridad backend
JWT_SECRET=<genera-un-secreto-largo>

# WhatsApp / Evolution API
EVOLUTION_API_URL=https://<tu-evolution-api>.up.railway.app
EVOLUTION_API_KEY=<tu-api-key>
EVOLUTION_INSTANCE_NAME=alma-movement

# ── Stripe Hosted Checkout ────────────────────────────────────────────────────
# Secret key from Stripe Dashboard → Developers → API Keys
STRIPE_SECRET_KEY=sk_test_...
# Webhook signing secret from Stripe Dashboard → Developers → Webhooks → endpoint
STRIPE_WEBHOOK_SECRET=whsec_...
# Shows on bank statements (max 22 chars)
STRIPE_STATEMENT_DESCRIPTOR=ALMA MOVEMENT
# Set true to allow sk_test_ in production. DELETE before going live.
PAYMENT_PROVIDER_ALLOW_TEST=false
